# Bağımlılıklar — npm install ile yeniden kurulur
node_modules/

# Expo / build çıktıları
.expo/
dist/
web-build/
*.tsbuildinfo

# Ortam değişkenleri — anahtarlar asla repoya girmez (.env.example girer)
.env
.env.*
!.env.example

# İşletim sistemi / editör
.DS_Store
Thumbs.db
*.log

# Claude Code yerel ayarları
.claude/settings.local.json
